---
import Layout from '../layouts/Layout.astro';
import { isAuthenticated } from '../lib/services/authService';

// Verificar si el usuario ya está autenticado
const token = Astro.cookies.get('sb-access-token')?.value;
const authenticated = await isAuthenticated(token);
if (authenticated) {
  return Astro.redirect('/dashboard');
}
---

<Layout title="Registro - SoftControl CRM" showNav={false}>
  <div class="auth-container">
    <div class="auth-branding">
      <div class="branding-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
        <div class="shape shape-4"></div>
      </div>

      <div class="branding-content">
        <div class="branding-logo">
          <div class="logo-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>
          <span class="logo-text">SoftControl</span>
        </div>

        <div class="branding-message">
          <h1>Crea una cuenta y acelera tus operaciones</h1>
          <p>Gestiona ventas, licencias y clientes en una sola plataforma diseñada para equipos modernos.</p>
        </div>

        <div class="branding-features">
          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <span>Control total de clientes</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v6a4 4 0 004 4h10a4 4 0 004-4V7H3z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 3H8v4h8V3z" />
              </svg>
            </div>
            <span>Licencias y suscripciones bajo control</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5h10M11 9h10M11 13h10m-4 4h4M5 7h.01M5 11h.01M5 15h.01" />
              </svg>
            </div>
            <span>Reportes y analíticas en tiempo real</span>
          </div>
        </div>
      </div>

      <div class="branding-footer">
        <p>&copy; 2024 SoftControl. Todos los derechos reservados.</p>
      </div>
    </div>

    <div class="auth-form-panel">
      <div class="form-container">
        <div class="form-header">
          <h2>Configura tu cuenta</h2>
          <p>Completa el formulario para comenzar a usar SoftControl CRM</p>
        </div>

        <form id="signupForm" class="auth-form">
          <div class="form-group">
            <label for="fullName">Nombre completo</label>
            <div class="input-wrapper">
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
              <input
                id="fullName"
                name="fullName"
                type="text"
                placeholder="Nombre y Apellido"
                autocomplete="name"
                required
              />
            </div>
            <span class="error-message" id="fullNameError"></span>
          </div>

          <div class="form-group">
            <label for="email">Correo electrónico</label>
            <div class="input-wrapper">
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <input
                id="email"
                name="email"
                type="email"
                placeholder="tu@empresa.com"
                autocomplete="email"
                required
              />
            </div>
            <span class="error-message" id="emailError"></span>
          </div>

          <div class="form-group">
            <label for="password">Contraseña</label>
            <div class="input-wrapper">
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
              </svg>
              <input
                id="password"
                name="password"
                type="password"
                placeholder="••••••••"
                autocomplete="new-password"
                required
              />
            </div>
            <div class="password-strength" id="passwordStrength"></div>
            <span class="error-message" id="passwordError"></span>
          </div>

          <div class="form-group">
            <label for="confirmPassword">Confirmar contraseña</label>
            <div class="input-wrapper">
              <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <input
                id="confirmPassword"
                name="confirmPassword"
                type="password"
                placeholder="Repite tu contraseña"
                autocomplete="new-password"
                required
              />
            </div>
            <span class="error-message" id="confirmPasswordError"></span>
          </div>

          <label class="checkbox-label">
            <input type="checkbox" name="terms" required />
            <span class="checkmark"></span>
            <span>Acepto los <a href="/terms">términos de servicio</a> y la <a href="/privacy">política de privacidad</a></span>
          </label>
          <span class="error-message" id="termsError"></span>

          <button type="submit" class="submit-btn" id="submitBtn">
            <span class="btn-text" id="buttonText">Crear cuenta</span>
            <span class="btn-loader hidden" id="loadingSpinner">
              <svg class="animate-spin" viewBox="0 0 24 24" fill="none">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
            </span>
            <svg class="btn-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
            </svg>
          </button>

          <div class="feedback-message error hidden" id="errorMessage">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span id="errorText"></span>
          </div>

          <div class="feedback-message success hidden" id="successMessage">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span id="successText"></span>
          </div>
        </form>

        <div class="form-divider">
          <span>¿Ya tienes una cuenta?</span>
        </div>

        <a href="/login" class="register-link">
          <span>Inicia sesión aquí</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" />
          </svg>
        </a>
      </div>
    </div>
  </div>

  <script>
    import { supabase } from '../lib/database/supabase';

    const form = document.getElementById('signupForm') as HTMLFormElement;
    const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement;
    const buttonText = document.getElementById('buttonText');
    const loadingSpinner = document.getElementById('loadingSpinner');
    const messageBox = document.getElementById('successMessage');
    const fullNameInput = document.getElementById('fullName') as HTMLInputElement;
    const emailInput = document.getElementById('email') as HTMLInputElement;
    const passwordInput = document.getElementById('password') as HTMLInputElement;
    const confirmPasswordInput = document.getElementById('confirmPassword') as HTMLInputElement;
    const termsInput = document.querySelector('input[name="terms"]') as HTMLInputElement;
    const passwordStrength = document.getElementById('passwordStrength');

    const fullNameError = document.getElementById('fullNameError');
    const emailError = document.getElementById('emailError');
    const passwordError = document.getElementById('passwordError');
    const confirmPasswordError = document.getElementById('confirmPasswordError');
    const termsError = document.getElementById('termsError');
    const errorMessage = document.getElementById('errorMessage');
    const errorText = document.getElementById('errorText');
    const successMessage = document.getElementById('successMessage');
    const successText = document.getElementById('successText');

    fullNameInput.addEventListener('blur', () => {
      const fullName = fullNameInput.value.trim();
      if (fullName && fullName.length < 3) {
        fullNameError.textContent = 'El nombre debe tener al menos 3 caracteres';
        fullNameInput.classList.add('error');
      } else if (fullName) {
        fullNameError.textContent = '';
        fullNameInput.classList.remove('error');
      }
    });

    emailInput.addEventListener('blur', () => {
      const email = emailInput.value.trim();
      if (email && !isValidEmail(email)) {
        emailError.textContent = 'Por favor, ingresa un correo válido';
        emailInput.classList.add('error');
      } else {
        emailError.textContent = '';
        emailInput.classList.remove('error');
      }
    });

    passwordInput.addEventListener('input', () => {
      const password = passwordInput.value;
      const strength = calculatePasswordStrength(password);
      updatePasswordStrengthIndicator(strength);

      if (password) {
        passwordError.textContent = '';
        passwordInput.classList.remove('error');
      }
    });

    passwordInput.addEventListener('blur', () => {
      const password = passwordInput.value;
      if (password && password.length < 8) {
        passwordError.textContent = 'La contraseña debe tener al menos 8 caracteres';
        passwordInput.classList.add('error');
      }
    });

    confirmPasswordInput.addEventListener('blur', () => {
      const password = passwordInput.value;
      const confirmPassword = confirmPasswordInput.value;
      if (confirmPassword && password !== confirmPassword) {
        confirmPasswordError.textContent = 'Las contraseñas no coinciden';
        confirmPasswordInput.classList.add('error');
      } else {
        confirmPasswordError.textContent = '';
        confirmPasswordInput.classList.remove('error');
      }
    });

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const fullName = fullNameInput.value.trim();
      const email = emailInput.value.trim();
      const password = passwordInput.value;
      const confirmPassword = confirmPasswordInput.value;
      const termsAccepted = termsInput.checked;

      fullNameError.textContent = '';
      emailError.textContent = '';
      passwordError.textContent = '';
      confirmPasswordError.textContent = '';
      termsError.textContent = '';
      fullNameInput.classList.remove('error');
      emailInput.classList.remove('error');
      passwordInput.classList.remove('error');
      confirmPasswordInput.classList.remove('error');
      errorMessage.classList.add('hidden');
      successMessage.classList.add('hidden');

      let isValid = true;

      if (!fullName || fullName.length < 3) {
        fullNameError.textContent = 'El nombre debe tener al menos 3 caracteres';
        fullNameInput.classList.add('error');
        isValid = false;
      }

      if (!email || !isValidEmail(email)) {
        emailError.textContent = 'Por favor, ingresa un correo válido';
        emailInput.classList.add('error');
        isValid = false;
      }

      if (!password || password.length < 8) {
        passwordError.textContent = 'La contraseña debe tener al menos 8 caracteres';
        passwordInput.classList.add('error');
        isValid = false;
      }

      if (password !== confirmPassword) {
        confirmPasswordError.textContent = 'Las contraseñas no coinciden';
        confirmPasswordInput.classList.add('error');
        isValid = false;
      }

      if (!termsAccepted) {
        termsError.textContent = 'Debes aceptar los términos de servicio';
        isValid = false;
      }

      if (!isValid) return;

      submitBtn.disabled = true;
      buttonText.classList.add('hidden');
      loadingSpinner.classList.remove('hidden');

      try {
        const response = await fetch('/api/auth/signup', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            fullName,
            email,
            password,
          }),
        });

        const data = await response.json();

        if (data.success) {
          successMessage.classList.remove('hidden');
          successText.textContent = 'Registro exitoso. Redirigiendo...';
          setTimeout(() => {
            window.location.href = '/dashboard';
          }, 1500);
        } else {
          errorMessage.classList.remove('hidden');
          errorText.textContent = data.error || 'Error al registrarse';
        }
      } catch (error) {
        errorMessage.classList.remove('hidden');
        errorText.textContent = 'Error de conexión. Por favor, intenta nuevamente.';
        console.error('Error:', error);
      } finally {
        submitBtn.disabled = false;
        buttonText.classList.remove('hidden');
        loadingSpinner.classList.add('hidden');
      }
    });

    function isValidEmail(email: string) {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      return emailRegex.test(email);
    }

    function calculatePasswordStrength(password: string) {
      let strength = 0;
      if (password.length >= 8) strength++;
      if (password.length >= 12) strength++;
      if (/[a-z]/.test(password) && /[A-Z]/.test(password)) strength++;
      if (/\d/.test(password)) strength++;
      if (/[!@#$%^&*]/.test(password)) strength++;
      return strength;
    }

    function updatePasswordStrengthIndicator(strength: number) {
      if (!passwordStrength) return;
      passwordStrength.innerHTML = '';
      passwordStrength.classList.remove('visible');

      if (strength === 0) return;

      passwordStrength.classList.add('visible');
      const colors = ['#ff4d6d', '#f97316', '#eab308', '#84cc16', '#22c55e'];
      const labels = ['Muy débil', 'Débil', 'Normal', 'Fuerte', 'Muy fuerte'];

      for (let i = 0; i < strength; i++) {
        const bar = document.createElement('div');
        bar.className = 'strength-bar';
        bar.style.backgroundColor = colors[i];
        passwordStrength.appendChild(bar);
      }

      const label = document.createElement('span');
      label.className = 'strength-label';
      label.textContent = labels[strength - 1];
      label.style.color = colors[strength - 1];
      passwordStrength.appendChild(label);
    }
  </script>

  <style>
    .auth-container {
      display: flex;
      min-height: 100vh;
      background: #05060b;
    }

    .auth-branding {
      flex: 1;
      background: linear-gradient(135deg, #05060b 0%, #101124 50%, #080a18 100%);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 2.5rem;
      position: relative;
      overflow: hidden;
      border-right: 1px solid rgba(0, 255, 242, 0.15);
    }

    .branding-shapes {
      position: absolute;
      inset: 0;
      pointer-events: none;
    }

    .shape {
      position: absolute;
      border-radius: 50%;
      filter: blur(60px);
    }

    .shape-1 {
      width: 320px;
      height: 320px;
      top: -80px;
      right: -40px;
      background: radial-gradient(circle, rgba(0, 255, 242, 0.3), transparent);
      animation: float 8s ease-in-out infinite;
    }

    .shape-2 {
      width: 220px;
      height: 220px;
      bottom: 15%;
      left: -60px;
      background: radial-gradient(circle, rgba(255, 0, 255, 0.25), transparent);
      animation: float 10s ease-in-out infinite reverse;
    }

    .shape-3 {
      width: 160px;
      height: 160px;
      top: 45%;
      right: 15%;
      background: radial-gradient(circle, rgba(0, 255, 136, 0.25), transparent);
      animation: float 6s ease-in-out infinite;
    }

    .shape-4 {
      width: 120px;
      height: 120px;
      bottom: 5%;
      right: 30%;
      background: radial-gradient(circle, rgba(0, 180, 255, 0.25), transparent);
      animation: float 7s ease-in-out infinite reverse;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0) rotate(0deg); }
      50% { transform: translateY(-20px) rotate(4deg); }
    }

    .branding-content {
      position: relative;
      z-index: 1;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .branding-logo {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 3rem;
    }

    .branding-logo .logo-icon {
      width: 52px;
      height: 52px;
      background: linear-gradient(135deg, #00fff2, #ff00ff);
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 30px rgba(0, 255, 242, 0.5), 0 0 60px rgba(255, 0, 255, 0.3);
    }

    .branding-logo svg {
      width: 28px;
      height: 28px;
      color: white;
    }

    .logo-text {
      font-size: 1.8rem;
      font-weight: 800;
      background: linear-gradient(90deg, #00fff2, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: -0.02em;
    }

    .branding-message h1 {
      font-size: 2.5rem;
      font-weight: 800;
      color: white;
      line-height: 1.2;
      margin-bottom: 1rem;
    }

    .branding-message p {
      font-size: 1.125rem;
      color: #94a3b8;
      line-height: 1.6;
      max-width: 420px;
    }

    .branding-features {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
      margin-top: 3rem;
    }

    .feature-item {
      display: flex;
      align-items: center;
      gap: 0.875rem;
      color: #cbd5e1;
      font-weight: 500;
    }

    .feature-icon {
      width: 40px;
      height: 40px;
      min-width: 40px;
      background: rgba(0, 255, 242, 0.08);
      border: 1px solid rgba(0, 255, 242, 0.3);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #00fff2;
    }

    .feature-icon svg {
      width: 20px;
      height: 20px;
    }

    .branding-footer {
      position: relative;
      z-index: 1;
      color: #64748b;
      font-size: 0.9rem;
    }

    .auth-form-panel {
      flex: 1;
      display: flex;
      align-items: flex-start;
      justify-content: center;
      padding: 2rem;
      overflow-y: auto;
      background: #05060b;
      background-image:
        linear-gradient(rgba(0, 255, 242, 0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 255, 242, 0.02) 1px, transparent 1px);
      background-size: 50px 50px;
    }

    .form-container {
      width: 100%;
      max-width: 460px;
      margin: auto 0;
      background: rgba(255, 255, 255, 0.035);
      border: 1px solid rgba(0, 255, 242, 0.2);
      border-radius: 22px;
      padding: 2rem;
      backdrop-filter: blur(18px);
      box-shadow: 0 0 40px rgba(0, 0, 0, 0.5), 0 0 80px rgba(0, 255, 242, 0.08);
    }

    .form-header {
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .form-header h2 {
      font-size: 1.5rem;
      font-weight: 800;
      color: white;
      margin-bottom: 0.5rem;
    }

    .form-header p {
      color: #94a3b8;
      font-size: 1rem;
    }

    .auth-form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.35rem;
    }

    .form-group label {
      font-size: 0.875rem;
      font-weight: 600;
      color: #cbd5e1;
    }

    .input-wrapper {
      position: relative;
      display: flex;
      align-items: center;
    }

    .input-wrapper input {
      width: 100%;
      padding: 0.75rem 1rem 0.75rem 2.5rem;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(0, 255, 242, 0.15);
      border-radius: 10px;
      font-size: 0.95rem;
      color: white;
      transition: all 0.3s;
    }

    .input-wrapper input::placeholder {
      color: rgba(255, 255, 255, 0.35);
    }

    .input-wrapper input:focus {
      outline: none;
      border-color: #00fff2;
      background: rgba(0, 255, 242, 0.05);
      box-shadow: 0 0 20px rgba(0, 255, 242, 0.2), inset 0 0 20px rgba(0, 255, 242, 0.05);
    }

    .input-wrapper input.error {
      border-color: #ff4d6d;
    }

    .input-icon {
      position: absolute;
      left: 0.85rem;
      width: 18px;
      height: 18px;
      color: #00fff2;
      opacity: 0.5;
      pointer-events: none;
      transition: opacity 0.3s;
    }

    .input-wrapper input:focus + .input-icon {
      opacity: 1;
      filter: drop-shadow(0 0 5px #00fff2);
    }

    .error-message {
      font-size: 0.75rem;
      color: #ff4d6d;
      min-height: 0.875rem;
    }

    .password-strength {
      display: none;
      gap: 0.4rem;
      align-items: center;
      margin-top: 0.35rem;
      flex-wrap: wrap;
    }

    .password-strength.visible {
      display: flex;
    }

    .strength-bar {
      flex: 1;
      min-width: 40px;
      height: 4px;
      border-radius: 2px;
      background: rgba(255, 255, 255, 0.1);
    }

    .strength-label {
      font-size: 0.75rem;
      font-weight: 600;
    }

    .checkbox-label {
      display: flex;
      align-items: flex-start;
      gap: 0.5rem;
      color: #94a3b8;
      font-size: 0.8rem;
      cursor: pointer;
      line-height: 1.4;
    }

    .checkbox-label input {
      width: 16px;
      height: 16px;
      flex-shrink: 0;
      accent-color: #00fff2;
      margin-top: 2px;
    }

    .checkbox-label a {
      color: #00fff2;
      text-decoration: none;
      font-weight: 600;
    }

    .checkbox-label a:hover {
      text-shadow: 0 0 10px rgba(0, 255, 242, 0.4);
    }

    .submit-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      width: 100%;
      padding: 0.85rem 1.25rem;
      background: linear-gradient(135deg, rgba(0, 255, 242, 0.25), rgba(255, 0, 255, 0.25));
      border: 1px solid #00fff2;
      border-radius: 10px;
      font-size: 0.95rem;
      font-weight: 600;
      color: #00fff2;
      cursor: pointer;
      transition: all 0.3s;
      box-shadow: 0 0 25px rgba(0, 255, 242, 0.35), inset 0 0 20px rgba(0, 255, 242, 0.1);
    }

    .submit-btn:hover:not(:disabled) {
      transform: translateY(-2px);
      background: linear-gradient(135deg, rgba(0, 255, 242, 0.35), rgba(255, 0, 255, 0.35));
      box-shadow: 0 0 35px rgba(0, 255, 242, 0.5), inset 0 0 25px rgba(0, 255, 242, 0.15);
    }

    .submit-btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .btn-arrow {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
      transition: transform 0.3s;
    }

    .submit-btn:hover .btn-arrow {
      transform: translateX(4px);
    }

    .btn-loader svg {
      width: 18px;
      height: 18px;
    }

    .hidden {
      display: none !important;
    }

    .feedback-message {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem;
      border-radius: 12px;
      font-size: 0.95rem;
      font-weight: 500;
      backdrop-filter: blur(10px);
      animation: slideIn 0.3s ease;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .feedback-message svg {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
    }

    .feedback-message.error {
      background: rgba(255, 77, 109, 0.08);
      color: #ff4d6d;
      border: 1px solid rgba(255, 77, 109, 0.4);
    }

    .feedback-message.success {
      background: rgba(0, 255, 136, 0.08);
      color: #00ff88;
      border: 1px solid rgba(0, 255, 136, 0.4);
    }

    .form-divider {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin: 1rem 0;
    }

    .form-divider::before,
    .form-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: rgba(0, 255, 242, 0.2);
    }

    .form-divider span {
      color: #64748b;
      font-size: 0.9rem;
    }

    .register-link {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      width: 100%;
      padding: 0.75rem 1.25rem;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 0, 255, 0.25);
      border-radius: 10px;
      font-size: 0.95rem;
      font-weight: 600;
      color: #cbd5e1;
      text-decoration: none;
      transition: all 0.3s;
    }

    .register-link svg {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
    }

    .register-link:hover {
      color: #ff00ff;
      border-color: #ff00ff;
      background: rgba(255, 0, 255, 0.12);
      box-shadow: 0 0 25px rgba(255, 0, 255, 0.2);
    }

    @media (max-width: 1024px) {
      .auth-branding {
        display: none;
      }

      .auth-form-panel {
        flex: 1;
      }
    }

    @media (max-width: 520px) {
      .auth-form-panel {
        padding: 1.5rem;
      }

      .form-container {
        padding: 1.75rem;
      }

      .form-header h2 {
        font-size: 1.6rem;
      }
    }
  </style>
</Layout>
